{
  "name": "startupperks",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro sync && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "dev:submit-api": "bunx wrangler dev --config workers/submit-api/wrangler.toml",
    "deploy:submit-api": "bunx wrangler deploy --config workers/submit-api/wrangler.toml"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^5.17.1"
  }
}
